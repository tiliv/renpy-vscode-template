name: Pytest & Coverage
on: [push]
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Poetry
        uses: abatilo/actions-poetry@v2.1.3
      - name: Test
        run: |
          poetry run pytest
